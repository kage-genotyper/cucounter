
name: Build and test

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.10"]
    steps:
    - uses: actions/checkout@v3
    - name: Install CUDA Toolkit
      uses: Jimver/cuda-toolkit@v0.2.8
      id: cuda-toolkit
      with:
        cuda: '11.7.0'
    - name: Check CUDA version
      run: nvcc -V
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: "${{ matrix.python-version }}"
    - name: Locate libcuda.so.1
      run: locate libcuda.so.1
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install numpy
        pip install cupy-cuda11x
        pip install pytest
    - name: Run pytest
      run: |
        ./run_tests

